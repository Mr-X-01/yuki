# Yuki - Необнаруживаемый VPN-сервис

**Стелс VPN для обхода блокировок в России** с протоколом gRPC поверх TLS.

Трафик замаскирован под обычный HTTPS и не определяется DPI системами.

---

## 📋 Содержание

1. [Быстрый старт](#-быстрый-старт)
2. [Установка сервера](#-установка-сервера)
3. [Установка клиента Windows](#-установка-клиента-windows)
4. [Использование](#-использование)
5. [Устранение проблем](#-устранение-проблем)

---

## ⚡ Быстрый старт

### Что нужно:

- **Сервер**: Ubuntu 24 (VPS с белым IP)
- **Домен**: Любой домен с A-записью на IP сервера
- **Email**: Для получения SSL сертификата
- **Клиент**: Windows 10/11 (64-bit)

### За 5 минут:

1. Запустите установку на сервере
2. Создайте клиента в админ-панели
3. Скачайте конфигурацию
4. Подключитесь!

---

## 🖥️ Установка сервера

### 1. Подготовка домена

Создайте A-запись для вашего домена:
```
Тип: A
Имя: @ (или api, vpn, и т.д.)
Значение: IP_ВАШЕГО_СЕРВЕРА
TTL: 3600
```

Подождите 5-10 минут пока DNS обновится.

### 2. Запуск установки

Подключитесь к серверу по SSH:
```bash
ssh root@ваш-сервер
```

Запустите автоматическую установку:
```bash
curl -fsSL https://raw.githubusercontent.com/Mr-X-01/yuki/main/scripts/install.sh | bash
```

**Или вручную:**
```bash
git clone https://github.com/Mr-X-01/yuki.git /opt/yuki
cd /opt/yuki/scripts
sudo bash install.sh
```

### 3. Введите данные при установке

Скрипт спросит:
- **Домен**: `vpn.example.com` (ваш домен)
- **Email**: `admin@example.com` (для SSL)
- **Логин админа**: `admin` (для веб-панели)
- **Пароль админа**: `сложный_пароль` (для веб-панели)

### 4. Проверка установки

После установки проверьте:
```bash
# Проверка статуса сервера
sudo systemctl status yuki

# Проверка логов
sudo journalctl -u yuki -f

# Проверка здоровья API
curl https://ваш-домен.ru/health
```

Должно вернуть: `{"status":"ok"}`

### 5. Вход в админ-панель

Откройте в браузере:
```
https://ваш-домен.ru/admin
```

Введите логин и пароль из шага 3.

---

## 💻 Установка клиента Windows

### Шаг 1: Скачать клиент

1. Перейдите в [Releases](https://github.com/Mr-X-01/yuki/releases)
2. Скачайте `yuki-client-windows-amd64.zip`
3. Распакуйте в любую папку (например, `C:\Yuki`)

### Шаг 2: Установить WinTun драйвер

Откройте PowerShell **от имени администратора** и выполните:
```powershell
cd C:\Yuki
.\install-wintun.ps1
```

Должно появиться:
```
✅ WinTun установлен успешно
```

### Шаг 3: Создать клиента в админ-панели

1. Войдите в админ-панель: `https://ваш-домен.ru/admin`
2. Нажмите **"Создать клиента"**
3. Введите имя (например, `laptop-home`)
4. Нажмите **"Скачать конфигурацию"**
5. Сохраните файл как `yuki.json` в папку с клиентом

### Шаг 4: Запустить клиент

**Вариант 1: Через конфигурационный файл**
```powershell
cd C:\Yuki
.\yuki-client.exe
```

**Вариант 2: Через URI (быстрый запуск)**
```powershell
.\yuki-client.exe -uri "yuki://CLIENT_ID:SECRET@домен.ru:8443?encryption=xchacha20-poly1305"
```

### Шаг 5: Проверить подключение

После запуска должно появиться:
```
✅ URI успешно обработан
🌸 Yuki VPN Client Starting...
✅ Tunnel connected!
🌐 Interface: Yuki Tunnel (10.0.0.2/24)
🚀 Весь трафик теперь направляется через VPN туннель
```

Проверьте IP:
```powershell
curl https://2ip.ru/api/info
```

Должен показать IP вашего сервера.

---

## 🎯 Использование

### Создание дополнительных клиентов

1. Откройте админ-панель
2. Нажмите **"Создать клиента"**
3. Укажите уникальное имя
4. Скачайте конфигурацию

### Удаление клиента

1. Откройте админ-панель
2. Найдите клиента в списке
3. Нажмите **"Удалить"**

### Просмотр статистики

В админ-панели отображается:
- Список активных клиентов
- Время последнего подключения
- Объем переданных данных

### Остановка клиента

Нажмите `Ctrl+C` в окне PowerShell где запущен клиент.

### Автозапуск клиента

Создайте ярлык с командой:
```
C:\Yuki\yuki-client.exe -uri "yuki://..."
```

И поместите его в:
```
C:\Users\ВАШ_ПОЛЬЗОВАТЕЛЬ\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup
```

---

## 🔧 Устранение проблем

### Сервер

#### Проблема: "Сервер не отвечает"

**Проверка 1: DNS записи**
```bash
nslookup ваш-домен.ru
```
Должен вернуть IP вашего сервера.

**Проверка 2: Nginx слушает порты**
```bash
sudo netstat -tlnp | grep -E ':(80|443)'
```
Должно показать nginx на портах 80 и 443.

**Проверка 3: SSL сертификат**
```bash
sudo ls -la /etc/letsencrypt/live/ваш-домен.ru/
```
Должны быть файлы `fullchain.pem` и `privkey.pem`.

**Проверка 4: Yuki сервер запущен**
```bash
sudo systemctl status yuki
```

**Проверка 5: Логи сервера**
```bash
sudo journalctl -u yuki -n 50
```

#### Проблема: "Туннель подключается, но нет интернета"

**На сервере выполните:**
```bash
# Проверка IP forwarding
sysctl net.ipv4.ip_forward
# Должно быть: net.ipv4.ip_forward = 1

# Если нет, включите:
echo "net.ipv4.ip_forward=1" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p

# Проверка NAT правил
sudo iptables -t nat -L -n -v

# Если NAT нет, добавьте:
INTERFACE=$(ip route | grep default | awk '{print $5}' | head -n1)
sudo iptables -t nat -A POSTROUTING -s 10.0.0.0/24 -o $INTERFACE -j MASQUERADE
sudo iptables -A FORWARD -s 10.0.0.0/24 -j ACCEPT
sudo iptables -A FORWARD -d 10.0.0.0/24 -m state --state RELATED,ESTABLISHED -j ACCEPT

# Сохраните правила
sudo netfilter-persistent save
```

### Клиент Windows

#### Проблема: "WinTun DLL not loaded"

**Решение:**
```powershell
# Перекомпилируйте клиент
cd C:\Yuki\client-windows
go build -o yuki-client.exe .
```

#### Проблема: "Cannot create a file when that file already exists"

**Решение:**
```powershell
# Удалите старые адаптеры WinTun
# Откройте: Панель управления > Сеть и Интернет > Сетевые подключения
# Удалите все адаптеры "Yuki Tunnel" или "sing-tun Tunnel"
```

#### Проблема: "Нет прав администратора"

**Решение:**
Запустите PowerShell от имени администратора:
1. Win + X
2. Выберите "Windows PowerShell (администратор)"

---

## 📚 Дополнительная информация

- [Архитектура](docs/ARCHITECTURE.md) - Как работает система
- [Сборка](BUILD.md) - Сборка из исходников
- [API документация](docs/API.md) - REST API для интеграции

---

## 🔐 Безопасность

- **Шифрование**: XChaCha20-Poly1305 (256-bit)
- **TLS**: TLSv1.2 и TLSv1.3
- **Аутентификация**: UUID + Secret key
- **Стелс**: Трафик замаскирован под gRPC/HTTPS

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте раздел [Устранение проблем](#-устранение-проблем)
2. Посмотрите логи: `sudo journalctl -u yuki -f`
3. Создайте Issue на GitHub

---

**Yuki VPN** - Стелс VPN для свободного интернета 🌸ское использование
